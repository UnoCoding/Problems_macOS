/*
#include <iostream>
#include <set>
using  namespace std;
int main(){
    int T;
    cin>>T;
    while(T--){
        string order1,order2;
        cin>>order1>>order2;
        set<char> s1;
        set<char>s2;
        for(int i=0;i<order1.size();i++)
            s1.insert(order1[i]);
        for(int i=0;i<order2.size();i++)
            s2.insert(order2[i]);
        if(s1==s2)
            cout<<"YES"<<"\n";
        else cout<<"NO"<<"\n";
    }
}
*/
